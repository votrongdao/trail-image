{{define "heading"}}
{{! Same header ID and class as in post.hbs and post-tag.hbs }}
<header class="content simple">
	<!--googleoff: index-->
	<nav id="mobile-menu"></nav>
	<!--googleon: index-->
	<div id="top-bar">
		<h1>{{title}}</h1>
		<div id="mobile-menu-button">{{"menu-hamburger"|icon}}</div>
	</div>
</header>
{{end}}